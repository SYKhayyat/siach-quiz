export default { title: "SQL & Databases", desc: "SELECT, joins, keys, normalization, transactions.",
questions: [
{q: "Which clause filters <b>rows before grouping</b>?", choices: ["<code>HAVING</code>", "<code>WHERE</code>", "<code>ORDER BY</code>", "<code>SELECT</code>"], answer: 1, why: "<code>WHERE</code> filters input rows; <code>HAVING</code> filters groups after <code>GROUP BY</code>.", tag: "SELECT"},
{q: "Which query returns all columns from <code>users</code>?", choices: ["<code>SELECT * FROM users;</code>", "<code>GET users;</code>", "<code>SELECT users;</code>", "<code>FETCH * users;</code>"], answer: 0, why: "<code>SELECT * FROM t</code> is the full-table scan form (list columns explicitly in real code).", tag: "SELECT"},
{q: "What does <code>INNER JOIN</code> return?", choices: ["All rows from both tables", "Only rows with a match in both tables", "Only unmatched rows", "Cartesian product"], answer: 1, why: "Inner join keeps matched pairs; <code>LEFT JOIN</code> keeps all left rows plus nulls for misses.", tag: "Joins"},
{q: "Which join keeps <b>all left rows</b> even with no match?", choices: ["<code>INNER JOIN</code>", "<code>LEFT JOIN</code>", "<code>CROSS JOIN</code>", "<code>SEMI JOIN</code>"], answer: 1, why: "<code>LEFT JOIN</code> pads missing right sides with NULLs.", tag: "Joins"},
{q: "What is a primary key?", choices: ["Any indexed column", "Unique, non-null identifier of a row", "A foreign key", "The first column"], answer: 1, why: "PK enforces entity integrity: unique + NOT NULL, one per table.", tag: "Keys"},
{q: "What does a foreign key enforce?", choices: ["Uniqueness in its own table", "Referential integrity to a parent key", "Faster text search", "Non-null only"], answer: 1, why: "FK values must exist in the referenced PK (or be NULL), preventing orphans.", tag: "Keys"},
{q: "Which aggregate ignores NULLs (in standard SQL)?", choices: ["<code>COUNT(*)</code>", "<code>COUNT(col)</code>", "Neither", "Both count nulls"], answer: 1, why: "<code>COUNT(*)</code> counts rows; <code>COUNT(col)</code> counts non-null values in that column.", tag: "Aggregation"},
{q: "Where do aggregate filters go?", choices: ["<code>WHERE sum(x) &gt; 5</code>", "<code>HAVING sum(x) &gt; 5</code> (with GROUP BY)", "<code>ORDER BY sum</code>", "<code>LIMIT</code>"], answer: 1, why: "Aggregates cannot appear in <code>WHERE</code>; filter groups with <code>HAVING</code>.", tag: "Aggregation"},
{q: "What does <code>DISTINCT</code> do?", choices: ["Deletes rows", "Removes duplicate result rows", "Sorts", "Creates an index"], answer: 1, why: "<code>SELECT DISTINCT city FROM users</code> collapses duplicates.", tag: "SELECT"},
{q: "Which constraint forbids NULLs?", choices: ["<code>UNIQUE</code>", "<code>NOT NULL</code>", "<code>CHECK</code>", "<code>DEFAULT</code>"], answer: 1, why: "<code>NOT NULL</code> rejects missing values; <code>UNIQUE</code> still allows NULLs in many DBs.", tag: "Schema"},
{q: "1NF mainly requires…", choices: ["No transitive deps", "Atomic (single) values per cell, no repeating groups", "Every determinant is a key", "No nulls"], answer: 1, why: "First normal form bans multi-valued cells/lists in one column — split into rows or tables.", tag: "Normalization"},
{q: "2NF vs 3NF in one line?", choices: ["2NF removes partial deps on part of a composite key; 3NF removes transitive deps on non-keys", "They are identical", "2NF is about joins", "3NF bans all keys"], answer: 0, why: "2NF: every non-key depends on the whole key. 3NF: non-keys depend only on keys, not on other non-keys.", tag: "Normalization"},
{q: "What does ACID's <b>Atomicity</b> mean?", choices: ["Transactions are all-or-nothing", "Data is always available", "Isolation is off", "Schema never changes"], answer: 0, why: "Either all statements in the transaction commit or all roll back — no partial writes.", tag: "Transactions"},
{q: "Which isolation problem is a <b>dirty read</b>?", choices: ["Reading uncommitted data that may roll back", "Two reads disagreeing", "Phantom rows", "Lost connection"], answer: 0, why: "READ UNCOMMITTED allows seeing another txn's not-yet-committed writes.", tag: "Transactions"},
{q: "What does an index mainly speed up?", choices: ["All writes", "Lookups/filters/joins on indexed columns, at write cost", "Backups", "Network"], answer: 1, why: "B-tree indexes give O(log n) seeks but each INSERT/UPDATE must maintain them.", tag: "Indexes"},
{q: "Which pattern prevents SQL injection?", choices: ["String concatenation", "Parameterized queries / prepared statements", "Hiding error messages only", "Using SELECT *"], answer: 1, why: "Bound parameters separate code from data; concatenation lets attacker-supplied quotes break out.", tag: "Security"},
{q: "What does <code>GROUP BY city</code> do?", choices: ["Sorts by city", "Collapses rows into one row per city for aggregates", "Deletes duplicates permanently", "Creates a table"], answer: 1, why: "Grouping partitions rows so <code>COUNT(*) / AVG(..)</code> compute per city.", tag: "Aggregation"},
{q: "Result of <code>NULL = NULL</code>?", choices: ["TRUE", "FALSE", "NULL (unknown)", "Error"], answer: 2, why: "Three-valued logic: any comparison with NULL yields UNKNOWN; use <code>IS NULL</code>.", tag: "Semantics"},
{q: "Which is the correct order of clauses?", choices: ["<code>SELECT … FROM … WHERE … GROUP BY … HAVING … ORDER BY … LIMIT</code>", "<code>FROM … SELECT … ORDER … WHERE</code>", "<code>WHERE … SELECT … FROM</code>", "Any order works"], answer: 0, why: "Logical order is FROM → WHERE → GROUP → HAVING → SELECT → ORDER → LIMIT.", tag: "SELECT"},
{q: "What does <code>UNION</code> (vs <code>UNION ALL</code>) do?", choices: ["Keeps duplicates", "Combines results and removes duplicates", "Joins sideways", "Intersects"], answer: 1, why: "Plain <code>UNION</code> deduplicates (with a sort/hash cost); <code>UNION ALL</code> keeps everything and is faster.", tag: "SELECT"},
{q: "What does <code>WHERE name LIKE 'A%'</code> match?", choices: ["Exactly 'A%'", "Names starting with A (<code>%</code> = any run)", "Names ending with A", "Names containing only A"], answer: 1, why: "<code>%</code> matches any sequence, <code>_</code> exactly one char: <code>'_at'</code> finds cat/hat.", tag: "SELECT"},
{q: "How do you page results (skip 20, take 10)?", choices: ["<code>LIMIT 10 OFFSET 20</code>", "<code>SELECT TOP 10 SKIP 20</code>", "<code>PAGE 3</code>", "<code>SKIP 20 TAKE 10</code>"], answer: 0, why: "Standard form is <code>LIMIT n OFFSET m</code> (MySQL also allows <code>LIMIT m, n</code>).", tag: "SELECT"},
{q: "What is a correlated subquery?", choices: ["Two independent queries", "Inner query references the outer row, re-evaluated per row", "A join", "A syntax error"], answer: 1, why: "<code>WHERE EXISTS (SELECT 1 … WHERE o.id = outer.id)</code> runs per outer row — often rewritten as a join.", tag: "Subqueries"},
{q: "Why prefer <code>EXISTS</code> over <code>IN</code> with NULLs?", choices: ["Faster always", "<code>IN</code> with a NULL in the set can return UNKNOWN and filter everything out", "EXISTS sorts", "No reason"], answer: 1, why: "<code>x NOT IN (1, NULL)</code> is never TRUE; <code>NOT EXISTS</code> behaves as expected.", tag: "Subqueries"},
{q: "Which computes average salary per dept?", choices: ["<code>SELECT AVG(salary) FROM emp;</code>", "<code>SELECT dept, AVG(salary) FROM emp GROUP BY dept;</code>", "<code>SELECT dept WHERE AVG</code>", "<code>AVG BY dept</code>"], answer: 1, why: "Aggregate per group: every non-aggregated SELECT column must appear in GROUP BY.", tag: "Aggregation"},
{q: "What does <code>COUNT(DISTINCT city)</code> do?", choices: ["Counts rows", "Counts unique non-null cities", "Deletes duplicates", "Counts NULLs too"], answer: 1, why: "DISTINCT inside the aggregate collapses repeats before counting.", tag: "Aggregation"},
{q: "Correct <code>UPDATE</code> syntax?", choices: ["<code>UPDATE emp SET salary = salary*1.1 WHERE id = 7;</code>", "<code>UPDATE emp salary*1.1;</code>", "<code>SET emp.salary = …</code>", "<code>MODIFY emp …</code>"], answer: 0, why: "Always include WHERE — without it every row updates. Test with SELECT first.", tag: "DML"},
{q: "What do <code>COMMIT</code> / <code>ROLLBACK</code> do?", choices: ["Save to disk / delete table", "Make the transaction permanent / discard it", "Lock / unlock", "Same thing"], answer: 1, why: "Until COMMIT, other transactions don't see your writes (per isolation); ROLLBACK undoes them.", tag: "Transactions"},
{q: "Order the isolation levels weakest → strongest.", choices: ["SERIALIZABLE → REPEATABLE READ → READ COMMITTED → READ UNCOMMITTED", "READ UNCOMMITTED → READ COMMITTED → REPEATABLE READ → SERIALIZABLE", "All equal", "READ COMMITTED is strongest"], answer: 1, why: "Each step bans more anomalies (dirty → non-repeatable → phantom reads).", tag: "Transactions"},
{q: "What is a view?", choices: ["A table copy", "A saved query acting as a virtual table", "An index", "A backup"], answer: 1, why: "Views encapsulate logic and restrict columns; materialized views cache the result physically.", tag: "Schema"},
{q: "What does <code>ON DELETE CASCADE</code> do?", choices: ["Blocks deletes", "Auto-deletes child rows when the parent is deleted", "Nulls the PK", "Drops the table"], answer: 1, why: "Alternatives: RESTRICT (forbid), SET NULL, or handle orphans in app code.", tag: "Keys"},
{q: "A self-join is used for…", choices: ["Two databases", "Relating rows in the same table (e.g. employee → manager)", "Backups", "Unions"], answer: 1, why: "Alias the table twice: <code>FROM emp e JOIN emp m ON e.mgr = m.id</code>.", tag: "Joins"},
{q: "What does <code>CROSS JOIN</code> produce?", choices: ["Matched pairs", "Every combination (cartesian product)", "An error", "Distinct rows"], answer: 1, why: "m × n rows — occasionally intended (generators), usually a missing ON clause.", tag: "Joins"},
{q: "What does <code>COALESCE(a, b, 0)</code> return?", choices: ["Always 0", "The first non-NULL of a, b, else 0", "NULL always", "The sum"], answer: 1, why: "The standard NULL-fallback chain — also see <code>NULLIF</code> for the reverse.", tag: "Functions"},
{q: "Window function vs GROUP BY?", choices: ["Same", "Window (<code>ROW_NUMBER() OVER (…)</code>) keeps rows and adds computed columns; GROUP collapses", "Windows delete rows", "GROUP keeps rows"], answer: 1, why: "Rank each employee within their dept without losing the employee rows.", tag: "Windows"},
{q: "Which enforces 'age ≥ 0'?", choices: ["<code>CHECK (age &gt;= 0)</code>", "<code>DEFAULT 0</code>", "<code>UNIQUE(age)</code>", "<code>INDEX(age)</code>"], answer: 0, why: "CHECK constraints reject violating writes at the DB level — validate there, not just in the app.", tag: "Schema"},
{q: "What does <code>GRANT SELECT ON t TO analyst</code> do?", choices: ["Creates a table", "Gives read permission (least privilege)", "Deletes data", "Makes admin"], answer: 1, why: "Principle of least privilege: grant only needed verbs on needed objects; REVOKE to remove.", tag: "Security"},
{q: "One-to-many is modeled by…", choices: ["Two PKs in one table", "FK in the 'many' table pointing at the 'one' PK", "A view", "Duplicating rows"], answer: 1, why: "e.g. orders.customer_id → customers.id; many-to-many needs a junction table.", tag: "Modeling"},
{q: "<code>INTERSECT</code> and <code>EXCEPT</code> do what?", choices: ["Join sideways", "Set intersection / difference of query results", "Sort", "Create tables"], answer: 1, why: "Rows in both (INTERSECT) vs rows in first but not second (EXCEPT/MINUS).", tag: "SELECT"},
{q: "Why is <code>SELECT *</code> discouraged in prod?", choices: ["Slower to type", "Breaks on schema change, wastes bandwidth, defeats covering indexes", "Returns too few rows", "It is deprecated"], answer: 1, why: "Name columns explicitly — stable contracts, less I/O, index-only scans.", tag: "SELECT"}
]};
